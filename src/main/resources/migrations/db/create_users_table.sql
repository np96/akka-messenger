CREATE TABLE "users" (
                login VARCHAR(256) NOT NULL PRIMARY KEY,
                first_name VARCHAR(256),
                last_name VARCHAR(256),
                status VARCHAR(256)
            )